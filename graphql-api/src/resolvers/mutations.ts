import { GraphQLObjectType } from "graphql";

export default new GraphQLObjectType({
  name: "Mutation",
  fields: {
    // createGateway
    // updateGateway
    // deleteGateway
    // createPeripheral
    // updatePeripheral
    // deletePeripheral
  },
});
